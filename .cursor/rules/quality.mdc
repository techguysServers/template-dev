---
description: Code quality — a11y, SEO, performance, best practices (all domains)
globs: "{landing-page,frontend,backend}/**/*.{ts,tsx}"
alwaysApply: false
---

# Quality standards

## Accessibility (WCAG 2.1 AA)

- Interactive elements must have accessible labels (`aria-label`, `aria-labelledby`, or visible text)
- Images must have meaningful `alt` text (empty `alt=""` for decorative images)
- Keyboard navigation must work: focus rings must be visible
- Color contrast ratio ≥ 4.5:1 for text
- Use semantic HTML (`<button>`, `<nav>`, `<main>`, `<section>`, `<article>`)

```tsx
// ❌
<div onClick={handleClick}>Click me</div>

// ✅
<button onClick={handleClick}>Click me</button>
```

## SEO

- Every page exports `metadata` or `generateMetadata`
- `<title>` and `<meta name="description">` always set
- Use Next.js `<Image>` component — never raw `<img>` for content images
- Heading hierarchy: one `<h1>` per page, then `<h2>` → `<h3>`

## Performance

- `"use client"` only when strictly needed (event handlers, hooks, browser APIs)
- Default to Server Components
- Dynamic imports for heavy components: `next/dynamic`
- No inline functions in JSX render for expensive computations → `useMemo` / `useCallback`

## Code hygiene

- No `console.log` in committed code
- No commented-out code blocks
- No `TODO` without a linked GitHub issue number
- Remove unused imports and variables before committing

## API / Backend quality

- Validate every input with Zod — body, params, query strings
- Return consistent error shapes: `{ error: { code, message } }`
- Use HTTP status codes correctly (200/201/400/401/403/404/422/500)
- No business logic in route handlers — extract to service functions
- No secrets or PII in logs

```ts
// ✅ Consistent error response
return c.json({ error: { code: "VALIDATION_ERROR", message: err.message } }, 422)

// ❌ Inconsistent
return c.json({ msg: "bad input" }, 400)
```

## Database

- Always use parameterized queries — never string concatenation in SQL
- Migrations versioned in `drizzle/migrations/` — never edit past migrations
- No raw SQL unless Drizzle can't express it; comment why
- Transactions for multi-step writes

## Testing

- Unit tests for all pure functions and utils
- Integration tests for all API routes (real DB, not mocked)
- E2E tests only for critical user journeys (login, signup, checkout)
- No `test.only` or `test.skip` in committed code
